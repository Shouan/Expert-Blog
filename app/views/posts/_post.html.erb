<li>
  <span class="content"><%= post.content %></span>
  <span class="timestamp">
    Posted <%= time_ago_in_words(post.created_at) %> ago.
  </span>
  <% if current_expert?(post.expert) %>
    <%= link_to "delete", post, method:  :delete,
                                     confirm: "You sure?",
                                     title:   post.content %>
  <% end %>
</li>